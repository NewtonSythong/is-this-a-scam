node_modules/
.next/
.env
.env.local
next-env.d.ts
*.tsbuildinfo
data/

.vercel
.env*
!.env.example

# benchmark answers already paid for — local to a machine, not a shared artefact
bench/.imc25-cache.jsonl

# benchmark answers already paid for — local to a machine, not a shared artefact
bench/.full-cache.jsonl
